import * as stylex from "@stylexjs/stylex";

export const cardActionMarker = stylex.defineMarker();
export const cardDescriptionMarker = stylex.defineMarker();
export const cardFooterMarker = stylex.defineMarker();
